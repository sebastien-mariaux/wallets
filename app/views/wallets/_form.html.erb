<%= form_for @wallet, url: url, method: method do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name, required: true %>
  <%= form.label :description %>
  <%= form.text_field :description %>
  <%= form.submit "Create" %>
<% end %>